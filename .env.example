# 不要把真实密钥提交到Git
APP_ENV=development
APP_BASE_URL=http://localhost

POSTGRES_DB=longyuan
POSTGRES_ADMIN_USER=postgres
POSTGRES_ADMIN_PASSWORD=change-me

# 生产中应使用不同数据库角色和最小权限
WEB_DATABASE_URL=postgresql://app_rw:change-me@postgres:5432/longyuan
WORKER_DATABASE_URL=postgresql://worker_candidate_rw:change-me@postgres:5432/longyuan
SCHEDULER_DATABASE_URL=postgresql://scheduler_jobs_rw:change-me@postgres:5432/longyuan

# 分别执行 `openssl rand -base64 32` 生成，至少 32 字符且不要复用
SESSION_SECRET=change-me
ENCRYPTION_KEY=change-me

# 模型API
KIMI_API_KEY=
DEEPSEEK_API_KEY=
QWEN_API_KEY=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=

# 对象存储 / 备份
S3_ENDPOINT=
S3_REGION=
S3_ACCESS_KEY_ID=
S3_SECRET_ACCESS_KEY=
S3_BACKUP_BUCKET=
S3_UPLOAD_BUCKET=

# 应用角色（web / worker / scheduler）
APP_ROLE=web
PORT=3000
HOSTNAME=0.0.0.0
UPLOAD_DIR=/app/uploads

# 监控
SENTRY_DSN=
