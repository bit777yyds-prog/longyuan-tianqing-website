# CLAUDE.md — 龙渊天青 Agent 原生网站

> 本文件是长期累积的项目规矩。每次纠错后追加一条，不删除历史条目。

## 1. 项目是什么

龙渊天青的作品、开放任务与 Agent 协作平台。前台公开书籍/任务/公司介绍；
后台由人与 Agent 共同读写**同一本事件总账**。核心是"事件溯源 + 候选/审批/投影"
三层写入纪律：Agent 只产生候选，人（审批服务账户）才写正式事件。

当前阶段：**实施计划待批准**。仓库只有 `docs/`、`db/init/` 与 `docker-compose.yml`；
`src/`、`tests/` 中除审计测试外尚无应用代码（见 `IMPLEMENTATION_PLAN.md` §2.3 的 S1–S11）。

## 2. 宪法红线（不可协商）

- Agent 不能直接写正式事件；Agent 只能写候选事件（`candidate_events`）。
- 正式事件（`event_ledger`）只增不改：禁止 UPDATE / DELETE / TRUNCATE；更正用更正事件。
- 外部可控内容（`contains_external_content=true`）**永久禁止自动批准**。
- Replay 使用存档 Run 输出重建状态，**不得重新调用模型**；重新调模型是 Rerun（新 Run/新候选）。
- `forbidden` 级数据不得发往第三方模型；出站前按字段分级检查。
- Prompt、Agent、模型配置语义**不可原地热改**：变化必须创建新版本。
- 成本超限、模型失败、dead-letter、输出校验失败、出站阻止**必须进入正式事件总账**。
- 生产/验收/记账/结算逻辑分离，任何主体不得验收自身产出（治理总纲宪法第十二条）。

## 3. `docs/` 各文件管什么

- `docs/product/龙渊天青_网站规划_v0.2.md` — 产品形态、A0–A3 自治级别定义（§见表）。
- `docs/product/龙渊天青_网站UI与交互规范_v0.1.md` — UI/交互/设计 Token。
- `docs/architecture/龙渊天青_Agent原生网站技术规格_v0.2.md` — 技术规格；**§26 = 验收测试判据**（§26.4 完整闭环为唯一判据）。
- `docs/architecture/review_precheck_output_schema_v0.2.json` — 验收初判 Agent 输出的 JSON Schema 与语言/安全策略。
- `docs/governance/龙渊天青_Agent原生架构技术审查裁定_v0.1.md` — 开工前置裁定（P0/P1 红线）。
- `docs/governance/龙渊天青_GEO与品牌解释权_v0.2.md` — 对外接口层规范（治理总纲分册四）。
- 注：《治理体系总纲》/宪法本体、《公开默认与必要保护原则》为上位文件，当前**不在本仓库**。

## 4. 审计者角色边界（本次验收）

- 审计者**只写测试、跑测试、出报告**（`TEST_REPORT.md` + `tests/`）。
- **不得**修改被测代码（`db/init/`、`src/`）；**不得**修改 `docs/` 下规格与治理文件；
  **不得**为让测试通过而放宽判据。
- 发现缺陷 = 写一个会失败的测试 + 一条报告条目，交回实现方（KimiCode）修复，
  修复后由审计者复跑确认转绿。
- 例外：`tests/` 下测试代码由审计者自由维护。
- 不得在同一会话内既修复缺陷又宣告该缺陷已验收。

## 5. 禁止事项

- 不修改被测代码 / 规格 / 治理文档。
- 不写入或提交任何真实密钥（`.env` 不提交；`.env.example` 里 `change-me` 保持占位）。
- 不连接生产数据库。
- 不删除任何数据（含测试库事件记录）；测试须用**独立数据库或事务回滚**。
- 不得放宽判据以求通过。

## 6. 纠错累积区（后续每次纠错追加一条）

- 2026-07-24：验收环境无 Docker/Postgres/Node/WSL；DB 级行为测试无法在审计机执行，
  测试已备好待实现方在具备 Postgres 的 CI 中运行。见 `TEST_REPORT.md` 执行说明。
- 2026-07-24（Phase 1A 复验）：迁移脚本 `003_apply_migrations.sh` 放在 `db/init/`（会挂进
  entrypoint），却依赖 `db/migrations/`（compose 未挂载）→ `docker compose up` 会**静默跳过**
  全部迁移仍打印成功。教训：凡「测试/CI 通过但部署路径不同」者必须单列核对；迁移要么随
  entrypoint 直接为 `db/init/NNN_*.sql`，要么在目录缺失时**报错退出**，禁止静默 no-op。
- 2026-07-24：被验收方（KimiCode）自撰的测试（如 `80_phase1a_governance.sql`）不得作为独立
  验收证据（宪法第十二条）；审计者须以自撰用例（`81_*` 等）独立复核。
- 2026-07-24（整改复验）：测试 fixture 若在真实授权（`002_init_roles.sh`）之后再 `GRANT`，会以
  「并集越权」**掩盖**最小权限验收。教训：fixture 只做角色存在性安全网，**绝不补授权**；权限行为
  必须只依赖被测的真实授权源。同理 harness 必须走与部署一致的路径（迁移放 `/db/migrations`，
  不用 `/tmp` + `MIGRATIONS_DIR` 覆盖），否则测试路径 ≠ 部署路径。
- 2026-07-24：实现方若改动审计者的检查/测试脚本，审计者须逐条复核「只增严、不削弱」，
  并保留独立自有用例作为验收基准。
